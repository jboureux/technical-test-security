# Technical test for analyzing and fixing technical debt & security issues

## Run with Docker:

Run: `docker compose up`

## Run in local with npm (npm is mandatory)

Run:

- `npm install`
- `npm run dev`
